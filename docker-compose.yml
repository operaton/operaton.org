services:
  jekyll:
    image: ruby:3.3.11
    working_dir: /site
    volumes:
      - .:/site
      - bundle-cache:/usr/local/bundle
    ports:
      - '4000:4000'
    command: bash -c "bundle install && bundle exec jekyll serve --host 0.0.0.0"

volumes:
  bundle-cache: